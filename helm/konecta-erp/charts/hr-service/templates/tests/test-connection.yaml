apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hr-service.fullname" . }}-test-connection"
  labels:
    {{- include "hr-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hr-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
