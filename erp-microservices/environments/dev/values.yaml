global:
  namespace: erp-dev
  environment: dev

postgresql:
  auth:
    password: dev-password
  persistence:
    size: 10Gi
    storageClass: gp3
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "250m"

discovery-server:
  replicaCount: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"

gateway-service:
  replicaCount: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"

auth-service:
  replicaCount: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"

hr-service:
  replicaCount: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"

finance-service:
  replicaCount: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"

reporting-service:
  replicaCount: 1
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"

frontend:
  replicaCount: 1
  resources:
    requests:
      memory: "128Mi"
      cpu: "50m"
